# Tennis Check 作業ガイド

## プロジェクト概要

Tennis Checkは、競技者向けのテニス練習カタログと個人チェックリストを提供するReactアプリです。

- React、TypeScript、Viteで構成する。
- 練習カタログは`data/`内の静的JSONで管理する。
- 個人の実施履歴と設定だけをZustandと`localStorage`へ保存する。
- GitHub Pagesのサブパスで公開するため、ルーティングと静的ファイルのパスはベースパスを考慮する。
- ローカル開発時は`#/editor`から練習項目、参考資料、コート配置を編集できる。

詳細仕様は次を参照する。

- 開発・公開方法: [`README.md`](README.md)
- 練習データの規則: [`data/README.md`](data/README.md)
- 画面・データ・コート図の設計: [`docs/DESIGN.md`](docs/DESIGN.md)

## 主なファイル

- `data/catalog.json`: カテゴリ別JSONの索引と項目数
- `data/drills/*.json`: 練習項目
- `data/sources.json`: 参考動画・資料
- `public/images/drills/`: 練習項目の参考画像
- `src/types/catalog.ts`: カタログの型定義
- `src/data/catalog.ts`: JSONの読み込みと項目・資料の関連付け
- `src/editor/validation.ts`: 編集時のデータ検証
- `src/components/court-diagram.tsx`: 共通コート図
- `src/pages/drill-detail-page.tsx`: 練習詳細画面

## 練習項目を追加・変更するとき

1. 既存項目、ID、名称、別名、タグ、参考資料を検索し、重複がないか確認する。
2. 通常は開発サーバーの`#/editor`を使い、対象カテゴリのJSONへ保存する。
3. 公開済みの`id`は変更しない。名称を変更しても同じIDを維持する。
4. 人数、環境、用具、強度、時間は自由タグではなく対応する構造化フィールドへ入れる。
5. 概要、セットアップ、手順、チェックポイント、成功条件、失敗例、安全上の注意の内容を互いに整合させる。
6. 参考資料は`data/sources.json`の既存IDを再利用するか、出典、提供者、媒体種別、確認箇所を伴う新規資料を登録する。
7. `sources`には内容の根拠、`media`には詳細画面で利用者へ表示する資料を指定する。資料で直接確認できない内容を`researched`として扱わない。
8. 参考画像を追加する場合は`public/images/drills/`へ保存し、内容を説明する代替テキストと出典クレジットを設定する。表示側では`import.meta.env.BASE_URL`を考慮する。
9. コート図のデータ座標は左上`(0, 0)`、右下`(100, 100)`を維持する。人物、球出し、用具、球道、移動、狙いの配置が説明文と一致することを確認する。
10. 新規項目を追加した場合は`data/catalog.json`のカテゴリ件数と合計件数を更新し、READMEなどに固定件数が書かれていれば整合させる。
11. 変更内容に応じてコンポーネントテストまたはデータ検証テストを追加・更新する。

## 完了前の確認

- `pnpm check`でlint、テスト、型チェック、ビルドを通す。
- 練習一覧から名称またはタグで検索し、追加・変更した項目の詳細画面を開く。
- デスクトップ幅とモバイル幅で、横方向のはみ出し、文字の重なり、画像の欠落がないことを確認する。
- コート図がある場合は、人物配置、軌道、狙い、フェーズ切替を確認する。
- 編集画面を変更した場合は、凡例、選択、クリック、ドラッグ、座標編集を確認する。
- 参考画像やリンクがある場合は、ローカル環境とGitHub Pagesのベースパス付きURLの両方を考慮する。
- `git diff`と`git status`を確認し、利用者の既存変更や無関係なファイルを含めない。

## 作業上の注意

- ユーザーの既存変更を消したり、無関係なファイルを整形したりしない。
- 医療・リハビリの処方を断定せず、痛みが関係する項目には安全上の注意を記載する。
- 動画や資料の内容を推測で補わず、必要な場面と説明が一致することを確認する。
- コミット、プッシュ、マージ、公開は、ユーザーが依頼した場合だけ行う。
