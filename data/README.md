# 練習カタログ

初回調査で108項目を収録した。`catalog.json`が入口で、大分類ごとのJSONを読み込む。

| 大分類 | 件数 |
|---|---:|
| 技術 | 46 |
| フットワーク・身体操作 | 12 |
| シングルス戦術 | 10 |
| ダブルス戦術 | 10 |
| フィジカル | 22 |
| メンタル・準備・回復 | 8 |
| 合計 | 108 |

## 参照の読み方

- `sources`: 内容を組み立てる際に参照した資料ID。
- `media`: 項目詳細で参考リンクとして表示する資料ID。`video-page`はページ内に動画がある。
- 実URL、提供組織、媒体種別、見る内容は`../data/sources.json`で管理する。
- `status: researched`: 資料中の練習・考え方に直接対応する。
- `status: derived`: 複数資料の原則から実行可能な練習へ構成した。名称はこのカタログ用。

## 編集ルール

1. `id`は公開後に変更しない。名称変更時も同じIDを使う。
2. 同じ動作でも目的や制約が違い、チェックポイントが変わるなら別項目にする。
3. 人数、環境、用具、強度、時間はタグへ混ぜず構造化フィールドにする。
4. 新しいタグを作る前に既存語を検索する。
5. 動画リンクには、項目と一致する動作または判断基準があるものを選ぶ。
6. 医療・リハビリ目的の処方にせず、痛みがある場合の注意を明記する。

## 今後詳細化するフィールド

初回は検索と一覧設計を優先した。アプリ実装と並行して、優先項目から次を追加する。

- `setup`: 人・球出し・用具の配置
- `steps`: 実施順序
- `successCriteria`: 回数、深さ、成功率など
- `commonErrors`: よくある失敗
- `progressions` / `regressions`: 難易度調整
- `diagram`: 正規化座標によるコートSVGデータ
- `media[].timestamp`: 動画内の見る位置

